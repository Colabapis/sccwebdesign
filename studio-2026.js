document.addEventListener('DOMContentLoaded',()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const header=document.querySelector('.site-header');const updateHeader=()=>header?.classList.toggle('is-scrolled',window.scrollY>18);updateHeader();window.addEventListener('scroll',updateHeader,{passive:true});document.querySelectorAll('.case-visual img,.site-window img,.portfolio-card img').forEach(img=>{const parent=img.closest('.case-visual,.site-window,.portfolio-card');const fail=()=>parent?.classList.add('media-failed');if(img.complete&&!img.naturalWidth)fail();img.addEventListener('error',fail,{once:true});});const reveal=[...document.querySelectorAll('[data-reveal]')];if(reduced){reveal.forEach(el=>el.classList.add('is-visible'));return;}const io=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target);}})},{threshold:.14});reveal.forEach(el=>io.observe(el));const stack=document.querySelector('.showcase-stack');if(stack){stack.addEventListener('pointermove',e=>{const r=stack.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;stack.querySelectorAll('.site-window').forEach((el,i)=>{const d=(i+1)*3;el.style.translate=`${x*d}px ${y*d}px`;});},{passive:true});stack.addEventListener('pointerleave',()=>stack.querySelectorAll('.site-window').forEach(el=>el.style.translate='0 0'));}});