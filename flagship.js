document.addEventListener('DOMContentLoaded',()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const menu=document.querySelector('[data-menu]');const nav=document.querySelector('[data-nav]');menu?.addEventListener('click',()=>{const open=nav?.classList.toggle('open');menu.setAttribute('aria-expanded',String(Boolean(open)))});document.querySelectorAll('[data-nav] a').forEach(a=>a.addEventListener('click',()=>{nav?.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));document.querySelectorAll('[data-reveal]').forEach(el=>el.classList.add('visible'));const frames=[...document.querySelectorAll('iframe[data-src]')];const loadFrame=f=>{if(f.getAttribute('src')||!f.dataset.src)return;f.src=f.dataset.src};if('IntersectionObserver'in window){const fio=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){loadFrame(e.target);fio.unobserve(e.target)}}),{rootMargin:'250px'});frames.forEach(f=>fio.observe(f))}else{frames.forEach(loadFrame)}if(!reduced){const eco=document.querySelector('.ecosystem');if(eco&&matchMedia('(pointer:fine)').matches){eco.addEventListener('pointermove',e=>{const r=eco.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;eco.style.setProperty('--px',`${x*5}px`);eco.style.setProperty('--py',`${y*5}px`)});eco.addEventListener('pointerleave',()=>{eco.style.setProperty('--px','0px');eco.style.setProperty('--py','0px')})}}});